# R8/ProGuard rules for building this library itself in release mode. These differ from
# consumer-rules.pro, which is inherited by the apps that CONSUME the library. Empty for
# now: the library has never been built with minifyEnabled=true (see buildTypes.release
# in build.gradle), so there is no real need yet.
